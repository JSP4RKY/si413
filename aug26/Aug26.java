public class Aug26 {
    static void findLits(Scanner in)
    {
        in.useDelimiter(""); //treat each character as a token - .next() will now return one character at a time

        boolean printing = false;
        while (in.hashNext())
        {
            char c = in.next().charAt(0);
            if (c == '"')
            {
                
            }
        }
    }
}
